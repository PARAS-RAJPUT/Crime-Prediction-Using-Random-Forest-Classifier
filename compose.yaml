services:
  crimepredictorbackend:
    image: crimepredictorbackend
    build:
      context: crime-predictor/backend
      dockerfile: ./Dockerfile
    environment:
      NODE_ENV: production
    ports:
      - 3000:3000
